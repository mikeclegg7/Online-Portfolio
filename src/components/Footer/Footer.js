import React from 'react';
import classes from './Footer.css';

//Return a little footer
const Footer = (props) => (
        <div className={classes.Footer}>
                <div>
                        <p>Created in React by Mike Clegg</p>
                </div>
        </div>
);

export default Footer;